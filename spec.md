# 入力デバイスオフ 仕様書

## 概要

掃除時などに、選択した入力デバイス（キーボードまたはマウス）だけを一時的に無効化するWindowsアプリ。
デバイスを片方だけ無効化できるため、もう片方を使って簡単に復帰できる。

---

## 背景・目的

- 既存の類似アプリはキーボードとマウスを両方オフにするため、復帰が面倒
- 有線キーボード（ゲーミング含む）はスイッチがなく、掃除中の誤入力を防ぎにくい
- マウスは無線が多くスイッチで切れるが、キーボードだけを無効化したいケースがある
- **デバイスを選んで無効化できる**ことがこのアプリの最大の特徴

---

## 機能一覧

| 機能 | 説明 |
|------|------|
| デバイス選択 | ラジオボタンでキーボード／マウスを選択 |
| 無効化 | 選択したデバイスの入力をブロック |
| 復帰（キーボード無効時） | 画面上の「解除する」ボタンをマウスでクリック |
| 復帰（マウス無効時） | 設定したショートカットキーを押す |
| ショートカット設定 | マウス選択時に復帰用キーを変更可能 |

---

## 復帰方法のルール

| 無効化したデバイス | 復帰方法 |
|------------------|---------|
| キーボード | 画面の「解除する」ボタン（マウスでクリック） |
| マウス | 復帰用ショートカットキー |

### 復帰用ショートカットキーの設定

- マウス選択時、メイン画面にショートカット設定欄が表示される
- デフォルト: `Ctrl + Shift + F12`
- 設定欄をクリックしてキーを押すと変更できる
- 無効化中もこのショートカットだけは常に有効

---

## UI設計

### 通常画面（キーボード選択時）320×310px

```
┌────────────────────────────────┐  ← インジゴのアクセントバー
│  入力デバイスオフ               │
│  無効化するデバイスを選んでね   │
│ ┌──────────────────────────┐   │
│ │  デバイス選択             │   │
│ │  ● キーボード             │   │
│ │  ○ マウス                │   │
│ └──────────────────────────┘   │
│  接続しているすべてのキーボード  │
│  が無効になります               │
│  [ 無効化する ]                 │
└────────────────────────────────┘
```

### 通常画面（マウス選択時）320×420px

```
┌────────────────────────────────┐
│  入力デバイスオフ               │
│  無効化するデバイスを選んでね   │
│ ┌──────────────────────────┐   │
│ │  デバイス選択             │   │
│ │  ○ キーボード             │   │
│ │  ● マウス                │   │
│ └──────────────────────────┘   │
│ ┌──────────────────────────┐   │
│ │  復帰ショートカット        │   │
│ │  ctrl+shift+f12          │   │
│ │  クリックして変更できるよ  │   │
│ └──────────────────────────┘   │
│  接続しているすべてのマウスが   │
│  無効になります                 │
│  [ 無効化する ]                 │
└────────────────────────────────┘
```

### 無効化中画面 320×300px（ダークテーマ・常に最前面）

```
┌────────────────────────────────┐  ← アンバーのアクセントバー
│  入力デバイスオフ               │  （ダーク背景）
│  無効化中                      │
│ ┌──────────────────────────┐   │
│ │  キーボード をブロック中   │   │
│ └──────────────────────────┘   │
│  [ 解除する ]                   │  ← キーボード無効時のみ表示
└────────────────────────────────┘
```

---

## 技術仕様

| 項目 | 内容 |
|------|------|
| 言語 | Python 3.x |
| GUI | tkinter（標準ライブラリ） |
| キーボードブロック | `keyboard` ライブラリ（低レベルフック） |
| マウスブロック | `pynput` ライブラリ（`mouse.Listener(suppress=True)`） |
| 対応OS | Windows 10 / 11 |
| 配布形式 | `入力デバイスオフ.exe`（1ファイル、Python不要） |
| ビルドツール | PyInstaller |
| アイコン | `icon.ico`（キーボード＋赤×、Pillowで生成） |

---

## 動作フロー

1. アプリ起動
2. ラジオボタンでデバイスを選択（デフォルト: キーボード）
3. マウス選択時は復帰ショートカットを確認・変更
4. 「無効化する」ボタンを押す
5. 選択デバイスの入力をブロック開始（画面はダークテーマに切り替わり、常に最前面表示）
6. 掃除が終わったら復帰操作（ボタンまたはショートカット）
7. ブロック解除 → 通常に戻る

---

## 注意事項

- 管理者権限が必要な場合あり（低レベルキーボードフック）
- Ctrl+Alt+Del などのシステムキーは完全にブロックできない場合あり
- 無効化中はウィンドウを閉じることができない

---

## ファイル構成

```
keybord-off/
├── spec.md          # この仕様書
├── main.py          # メインアプリ
├── icon.ico         # アプリアイコン
└── requirements.txt # 必要なライブラリ（keyboard, pynput, pillow, pyinstaller）
```
